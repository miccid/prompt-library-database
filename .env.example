# ============================================================================
# Prompt Library Database - Environment Configuration
# ============================================================================
#
# This file shows the structure of environment variables needed to run
# the Prompt Library Database application.
#
# INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Update the values in .env with your actual configuration
# 3. IMPORTANT: Never commit .env to version control - it's in .gitignore
# 4. Keep .env.example in the repository with placeholder values only
#
# ============================================================================

# Authentication Credentials
# ============================================================================
# Set your desired username for login
# Default: admin
# Recommendation: Use a strong, unique username in production
USERNAME=admin

# Set your desired password for login
# Default: admin
# WARNING: Change this immediately for any shared or production deployment
# Recommendation: Use a strong password with uppercase, lowercase, numbers, and special characters
PASSWORD=admin


# Database Configuration
# ============================================================================
# Path to the SQLite database file
# Default: prompts.db
# Note: The file will be created automatically if it doesn't exist
# Tip: You can use a relative path (prompts.db) or absolute path (/var/lib/prompts.db)
PROMPT_DB_FILE=prompts.db


# ============================================================================
# Security Notes
# ============================================================================
#
# 1. NEVER commit the .env file to version control
# 2. NEVER share your .env file with others
# 3. Use strong, unique credentials in production environments
# 4. Rotate credentials regularly for security
# 5. Consider using environment-specific configurations for different deployments
#
# ============================================================================
